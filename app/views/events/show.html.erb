<h3><%= @event.title %></h3>
<p><%= @event.review %></p>
<p><%= @event.rating %></p>
<p><%= @event.date %></p>
<p><%= link_to "Edit", edit_event_path(@event) %> | <%= link_to "Delete", event_path(@event), method: :delete %></p>
<%= render partial: '/comments/new', locals: {comment: @comment, event_id: @event.id} %>
